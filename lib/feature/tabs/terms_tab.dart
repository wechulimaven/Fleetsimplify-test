import 'package:flutter/material.dart';

class TermsTab extends StatelessWidget {
  const TermsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: const Text('Terms and Conditions'),
    ));
  }
}
