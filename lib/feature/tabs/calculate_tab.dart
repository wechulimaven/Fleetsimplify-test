import 'package:flutter/material.dart';

class CalculateTab extends StatelessWidget {
  const CalculateTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: const Text('Calculate'),
    ));
  }
}
// Divider(),